
<div style="text-align: center;">
<br>
<h1>Voici La Page De Tout Les Evénements</h1>
<br>

<div style="text-align: center;">
    <%= @event.each do |event| %>

    <br><span><h4> <%= event.title %> -------> 
                   <%= event.description %> >>>>>
                   <%= event.start_date%>
        
        <br><%= button_to "Lire", event_path(event.id), method: 'show', class: "btn btn-primary" %>

        <% if user_signed_in? %>
            <%= button_to "Editer", edit_event_path(event.id), method: 'get', class: "btn btn-secondary" %>
            <%= button_to "Supprimer", event_path(event.id), method: 'delete', class: "btn btn-danger" %>
        <% else %>
        <% end %>

</h4>


<br>

<% end %>



</div>